{
  "name": "bidsvue-demos",
  "private": true,
  "version": "0.1.0",
  "description": "Tutorials and demos for BIDSvue, compiled from Markdown into a styled static site.",
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "bun run scripts/build.ts",
    "preview": "bun run scripts/build.ts && bun run scripts/preview.ts"
  },
  "devDependencies": {
    "marked": "^14.1.4"
  }
}
